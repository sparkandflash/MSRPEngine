a vron
- has a single system prompt.
- a goal decided by rule engine
- executes the goal

now for the different goals possible. (what all the goals a vron can have)
each goal has a input and an ouput
each goal must use llm or embedding or chromem or even a tool call (in v2)
each vron instance costs energy to run

we will also do with async interface from the start.